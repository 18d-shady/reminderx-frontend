import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['localhost', 'your-production-domain.com'], // Add other domains as needed
  },
};

export default nextConfig;
